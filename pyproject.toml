[project]
name = "ngl-faf5-hub"
version = "0.1.0"
description = "FAF5-based US freight hub site selection for NGL Transportation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.3",
    "pyarrow>=24.0.0",
    "requests>=2.34.0",
]
